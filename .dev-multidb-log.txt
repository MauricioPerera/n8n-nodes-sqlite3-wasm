Listo. v1.1.0 multi-database completa y verificada.

**Gate (real):** `run_integration_gate` → **PASS**. `resolveDatabasePath` pura + 16 property-tests congelados con oráculo. Métricas: ciclomática 10, nesting 2, params 1, **líneas 33** — todas bajo 11/4/6/41. `lint_task_contract` OK.

**tsc / jest (antes → después):**
- `tsc --noEmit`: exit 0 → exit 0
- `jest`: **101 passed / 14 suites → 122 passed / 16 suites** (101 base no rotos + 16 resolvePath + 5 multidb)

**Build + dist:**
- `rm -rf dist` + cache borrado → `npx n8n-node build` ✓ (BUILD_EXIT=0)
- `require('./dist/nodes/Sqlite/Sqlite.node.js')` → `LOAD OK: ['Sqlite']`; `resolvePath.js` exporta `resolveDatabasePath`; todos los módulos tocados cargan → **sin imports rotos**

**Test a-d cubiertos** en `nodes/Sqlite/__tests__/multidb.integration.test.ts`:
- a) `baseDirectory` + `database` `a.db`/`b.db` por item → dos DBs independientes dentro del baseDir
- b) `databasePathOverride` absoluto fuera del baseDir → usa ese, ignora credencial
- c) `database` `../escape.db` → lanza `/escapes the base directory/`, no escribe fuera
- d) retrocompat: solo `databasePath` → idéntico a hoy
- bonus: nada configurado → `/no database configured/`

**Desviaciones (documentadas en código y reporte):**
1. `singleMultiRow` resuelve la DB del item 0 (un INSERT multi-fila no cruza DBs).
2. Transaction multi-DB = atomicidad **por-DB** (BEGIN/COMMIT/ROLLBACK por conexión); single-DB idéntico a antes. Cross-DB real requeriría ATTACH, fuera de scope.
3. `credentialTest` actualizado para `baseDirectory` (implícito al hacer `databasePath` opcional).
4. Params de nodo **top-level** (no `collection`) para leer con `getNodeParameter` 3-args como describe el spec.
5. `npx n8n-node lint`: 41 problems **pre-existentes** de n8n Cloud compat (`node:*`, `node-sqlite3-wasm`, raw throws en `connection.ts`, `dependencies`) — el nodo wasm-local los usa por diseño; mis 3 archivos siguen el mismo patrón. No es regresión ni parte del DoD.

Reporte completo en `.dev-multidb-report.md`. Sin commitear (no me lo pediste).
